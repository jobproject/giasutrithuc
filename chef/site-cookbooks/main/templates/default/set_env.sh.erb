#!/bin/sh

<% db_config = node[:giasu][:db] -%>
export DB_HOST=localhost
export DB_NAME=giasu
export DB_USER=<%= db_config[:user] %>
export DB_PASS=<%= db_config[:password] %>

alias domysql='mysql -h $DB_HOST -u $DB_USER -p${DB_PASS} $DB_NAME'
